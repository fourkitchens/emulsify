{% if not attributes %}
<div class="form-item form-item--radio">
  <legend class="heading-3">Options as Radio Buttons</legend>
  <ul class="form-item--radios">
{% endif %}
      {% for key, listItem in listItems.four %}
      <li class="form-item--radio__item"><label for="radio{{ key }}"><input id="radio{{ key }}" name="radio" type="radio" class="radio" checked="checked"> {{ listItem.title }}</label></li>
      {% endfor %}
{% if not attributes %}
  </ul>
</div>
{% endif %}
