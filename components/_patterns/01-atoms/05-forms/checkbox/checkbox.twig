{% if not attributes %}
<div class="form-item form-item--checkbox">
  <legend class="heading-3">Options as Checkboxes</legend>
  <ul class="form-item--checkboxes">
{% endif %}
      {% for key, listItem in listItems.four %}
        {% include "@atoms/05-forms/checkbox/_checkbox-item.twig" with {
          "checkbox_item": listItem.title
        } %}
      {% endfor %}
{% if not attributes %}
  </ul>
</div>
{% endif %}
